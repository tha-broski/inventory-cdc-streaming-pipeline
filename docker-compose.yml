services:
  postgres-source:
    image: postgres:17

    environment:
      POSTGRES_USER: cdc_user
      POSTGRES_PASSWORD: cdc_password
      POSTGRES_DB: inventory_source

    ports:
      - "5433:5432"

    volumes:
      - postgres_source_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cdc_user -d inventory_source"]
      interval: 5s
      timeout: 5s
      retries: 5
      
volumes: 
  postgres_source_data:

